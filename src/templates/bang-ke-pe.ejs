<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Bảng kê PE</title>
    <style>
        /* (Dùng chung CSS với mẫu CTC) */
        body { font-family: 'Times New Roman', Times, serif; font-size: 11pt; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th, td { border: 1px solid black; padding: 5px; text-align: center; }
        .header { text-align: center; font-weight: bold; }
    </style>
</head>
<body>
    <div class="header">
        <h4>PHỤ LỤC IX</h4>
        <h4>BẢNG KÊ KHAI HÀNG HÓA XUẤT KHẨU ĐẠT TIÊU CHÍ "PE"</h4>
    </div>

    <table>
        <thead>
            <tr>
                <th>STT</th>
                <th>Tên nguyên liệu</th>
                <th>Mã HS</th>
                <th>Tổng trị giá</th>
                <th>Tên nhà cung cấp</th>
                <th>Nước xuất xứ</th>
                <th colspan="2">Tờ khai HQNK / Hóa đơn VAT</th>
                <th colspan>2">C/O ưu đãi NK / Khai báo của NSX</th>
            </tr>
            <tr>
                <th colspan="6"></th>
                <th>Số</th>
                <th>Ngày</th>
                <th>Số</th>
                <th>Ngày</th>
            </tr>
        </thead>
        <tbody>
            <% coApp.items[0].materialsBreakdown.forEach((npl, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td><%= npl.name %></td>
                    <td><%= npl.hsCode %></td>
                    <td><%= npl.value.toFixed(2) %></td>
                    <td><%= npl.supplierName %></td>
                    <td><%= npl.originCountry %></td>
                    <td><%= npl.sourceRef %></td>
                    <td><%= npl.sourceDate.toLocaleDateString('vi-VN') %></td>
                    <td><%= npl.originatingCertRef %></td>
                    <td><%= npl.originatingCertDate ? npl.originatingCertDate.toLocaleDateString('vi-VN') : '' %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
    <p><strong>Kết luận:</strong> Hàng hóa đáp ứng tiêu chí "PE".</p>
</body>
</html>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Bảng kê RVC</title>
    <style>
        /* (Dùng chung CSS với mẫu CTC) */
        body { font-family: 'Times New Roman', Times, serif; font-size: 11pt; }
        table { width: 100%; border-collapse: collapse; margin-top: 15px; }
        th, td { border: 1px solid black; padding: 5px; text-align: center; }
        .header { text-align: center; font-weight: bold; }
        .rvc-calc { margin-top: 20px; border: 1px solid black; padding: 10px; }
    </style>
</head>
<body>
    <div class="header">
        <h4>PHỤ LỤC VIII</h4>
        <h4>BẢNG KÊ KHAI HÀNG HÓA XUẤT KHẨU ĐẠT TIÊU CHÍ "RVC"</h4>
    </div>

    <p><strong>Tên hàng hóa:</strong> <%= coApp.items[0].productName %></p>
    <p><strong>Mã HS của hàng hóa (6 số):</strong> <%= coApp.items[0].productHsCode %></p>
    <p><strong>Trị giá (FOB):</strong> <%= coApp.items[0].fobValue.toFixed(2) %> USD</p>

    <table>
        <thead>
            <tr>
                <th>STT</th>
                <th>Các loại chi phí</th>
                <th>Mã HS</th>
                <th colspan="2">Trị giá (USD)</th>
                <th>Nước xuất xứ</th>
                <th colspan="2">Tờ khai HQNK / Hóa đơn VAT</th>
            </tr>
            <tr>
                <th colspan="3"></th>
                <th>Có xuất xứ FTA</th>
                <th>Không có XX FTA</th>
                <th></th>
                <th>Số</th>
                <th>Ngày</th>
            </tr>
        </thead>
        <tbody>
            <% coApp.items[0].materialsBreakdown.forEach((npl, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td><%= npl.name %></td>
                    <td><%= npl.hsCode %></td>
                    <td><%= npl.isOriginating ? npl.value.toFixed(2) : '' %></td>
                    <td><%= !npl.isOriginating ? npl.value.toFixed(2) : '' %></td>
                    <td><%= npl.originCountry %></td>
                    <td><%= npl.sourceRef %></td>
                    <td><%= npl.sourceDate.toLocaleDateString('vi-VN') %></td>
                </tr>
            <% }) %>
            <tr>
                <td colspan="3"><strong>Cộng (I)</strong></td>
                <td><strong><%= coApp.items[0].totalValueOriginating.toFixed(2) %></strong></td>
                <td><strong><%= coApp.items[0].totalValueNonOriginating.toFixed(2) %></strong></td>
                <td colspan="3"></td>
            </tr>
        </tbody>
    </table>

    <div class="rvc-calc">
        <p><strong>b) Công thức tính RVC gián tiếp:</strong></p>
        <p>
            RVC = (Trị giá FOB - Trị giá VNM) / Trị giá FOB * 100%
        </p>
        <p>
            RVC = (<%= coApp.items[0].fobValue.toFixed(2) %> - <%= coApp.items[0].totalValueNonOriginating.toFixed(2) %>) / <%= coApp.items[0].fobValue.toFixed(2) %> * 100%
        </p>
        <p>
            <strong>RVC = <%= coApp.items[0].rvcPercent.toFixed(2) %> %</strong>
        </p>
    </div>
    
    <p><strong>Kết luận:</strong> Hàng hóa đáp ứng tiêu chí RVC <%= coApp.items[0].rvcPercent.toFixed(2) %> %.</p>
</body>
</html>